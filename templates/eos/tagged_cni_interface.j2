vlan {{ s_tag }}
   name {{ service_key }}
interface {{ interface }}
  switchport trunk allowed vlan add {{ s_tag }}
interface Vxlan1
   vxlan vlan {{ s_tag }} vni {{ vni }}
router bgp {{ asn }}
   vlan-aware-bundle {{ vlan_bundle }}
      vlan add {{ s_tag }}
