these playbooks are for setting up the environment in AWX

```
CONTROLLER_HOST=http://localhost:8080 CONTROLLER_USERNAME=admin CONTROLLER_PASSWORD="<PASSWORD>" ansible-playbook tower.yaml --ask-vault-pass
```
